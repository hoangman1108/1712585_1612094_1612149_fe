export const PATH = "/" || process.env.REACT_APP_PUBLIC_URL;

export const API_URL = process.env.REACT_APP_BACKEND_URL;

export const NODE_ENV = {
	DEVELOPMENT: 'development',
	PRODUCTION: 'production'
}